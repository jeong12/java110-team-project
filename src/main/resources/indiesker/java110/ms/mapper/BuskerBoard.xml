<?xml version="1.0" encoding="UTF-8"?>
<!-- MemberDao 클래스가 사용할 SQL이 들어 있는 파일 -->
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="indiesker.java110.ms.dao.BuskerBoardDao">




    <insert id="add" parameterType="BuskerBoard" useGeneratedKeys="true" keyColumn="bbno" keyProperty="no">
    insert into busk_board(titl,genre,city,cnt,sdt,edt,tel1,tel2,etc,cdt,phot,bno)
    values(#{titl},#{genre},#{city},#{cnt},#{sdt},#{edt},#{tel1},#{tel2},#{etc},now(),#{phot},#{bno})
    </insert>
</mapper>









